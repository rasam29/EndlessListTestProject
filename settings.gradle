pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven{
            url 'https://sdk.uxcam.com/android/'
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven{
            url 'https://sdk.uxcam.com/android/'
        }
    }
}
rootProject.name = "EndlessProject"
include ':app'
